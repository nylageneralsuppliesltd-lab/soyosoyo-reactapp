# Tenant Logos

Place the purchasing SACCO's logo files here.

Recommended filenames:
- `client-logo.png` (primary logo)
- `favicon.ico` (optional favicon override)

The app will try `/tenant-logos/client-logo.png` first and fall back to the default `/logo.png` in `public/` if not found.
