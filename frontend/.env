# API URL Configuration
# VITE_API_URL is intentionally unset to enable auto-detection
# Frontend will use relative paths when deployed on same domain as backend
# For local dev with separate ports, set to http://localhost:3000
